import { LicenseInfo, withSBErrorHandling } from '../types';
import { ScanbotBarcodeSDKModule } from './scanbotBarcodeSDKModule';

/**
 * @internal
 * @hidden
 */
export const ScanbotBarcodeSdkImpl = {
  initialize(config) {
    return withSBErrorHandling(async () => new LicenseInfo(await ScanbotBarcodeSDKModule.initialize(config)));
  },
  getLicenseInfo() {
    return withSBErrorHandling(async () => new LicenseInfo(await ScanbotBarcodeSDKModule.getLicenseInfo()));
  },
  cleanupStorage() {
    return withSBErrorHandling(ScanbotBarcodeSDKModule.cleanupStorage);
  },
  mockCamera(params) {
    return withSBErrorHandling(() => ScanbotBarcodeSDKModule.mockCamera(params));
  }
};
//# sourceMappingURL=SdkImpl.js.map