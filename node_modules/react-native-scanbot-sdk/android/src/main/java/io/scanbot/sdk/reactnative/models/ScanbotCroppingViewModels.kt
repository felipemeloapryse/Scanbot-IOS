package io.scanbot.sdk.reactnative.models

import android.graphics.Color
import android.graphics.PointF
import androidx.annotation.Keep
import io.scanbot.sdk.core.ImageRef
import io.scanbot.sdk.core.ImageRotation

object CroppingViewDefaultConfiguration {
    const val DEFAULT_EDGE_COLOR = Color.WHITE
    const val DEFAULT_EDGE_ON_LINE_COLOR = Color.WHITE
    const val DEFAULT_ANCHOR_POINTS_COLOR = Color.WHITE
}

@Keep
data class CroppingViewCroppedAreaResult(
    val polygon: List<PointF>,
    val rotation: ImageRotation,
    val sourceImage: ImageRef
)